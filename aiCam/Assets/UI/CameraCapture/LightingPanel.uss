/* ================================
   Issue #120/#74: ライティングパネルスタイル
   コンパクト版 - スクロール不要
================================ */

/* フルスクリーンバックドロップ（パネル外クリックで閉じる用） */
.lighting-panel-backdrop {
    position: absolute;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    background-color: rgba(0, 0, 0, 0.3);
    display: none;
}

.lighting-panel-backdrop.visible {
    display: flex;
}

/* オーバーレイ（パネルコンテナ） - 画面下50%に配置 */
.lighting-panel-overlay {
    position: absolute;
    left: 8px;
    right: 8px;
    top: 50%;        /* 画面上端から50%の位置から開始 */
    bottom: 8px;     /* 画面下端から8pxまで */
    background-color: transparent;
    display: none;
    flex-direction: column;
    justify-content: flex-end;  /* パネルを下揃え */
    align-items: stretch;
}

.lighting-panel-overlay.visible {
    display: flex;
}

/* メインパネル：コンテナ内に収まるよう自動調整、スクロール対応 */
.lighting-panel {
    width: 100%;
    max-height: 100%;  /* オーバーレイ内に収まる範囲で最大化 */
    background-color: rgba(20, 20, 20, 0.95);
    border-radius: 10px;
    padding: 10px;
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.2);
    flex-direction: column;
    overflow: scroll;
    flex-shrink: 1;    /* 必要に応じて縮小 */
}

/* ヘッダー */
.lighting-panel-header {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 6px;
    padding-bottom: 6px;
    border-bottom-width: 1px;
    border-bottom-color: rgba(255, 255, 255, 0.15);
    flex-shrink: 0;
    height: 20px;
}

.lighting-panel-title {
    font-size: 12px;
    color: white;
    -unity-font-style: bold;
}

.lighting-panel-close {
    width: 22px;
    height: 22px;
    border-radius: 11px;
    background-color: rgba(255, 255, 255, 0.15);
    border-width: 0;
    color: white;
    font-size: 12px;
    -unity-text-align: middle-center;
}

.lighting-panel-close:hover {
    background-color: rgba(255, 255, 255, 0.3);
}

.lighting-panel-close:active {
    background-color: rgba(76, 175, 255, 0.5);
}

/* セクション - コンパクト */
.lighting-section {
    margin-bottom: 6px;
    flex-shrink: 0;
}

.section-label {
    font-size: 9px;
    color: rgba(255, 255, 255, 0.6);
    margin-bottom: 2px;
    -unity-font-style: bold;
}

.section-label-small {
    font-size: 8px;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 2px;
}

/* プリセットボタン - コンパクト */
.preset-container {
    flex-direction: row;
    flex-wrap: wrap;
    margin: -1px;
    flex-shrink: 0;
}

.preset-button {
    flex-grow: 0;
    width: 48px;
    height: 22px;
    margin: 1px;
    border-radius: 5px;
    background-color: rgba(255, 255, 255, 0.1);
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.15);
    color: rgba(255, 255, 255, 0.8);
    font-size: 8px;
    -unity-text-align: middle-center;
}

.preset-button:hover {
    background-color: rgba(255, 255, 255, 0.2);
}

.preset-button:active {
    background-color: rgba(76, 175, 255, 0.4);
    border-color: rgba(76, 175, 255, 0.8);
}

.preset-button.preset-selected {
    background-color: rgba(76, 175, 255, 0.4);
    border-color: rgba(76, 175, 255, 1);
    color: white;
    -unity-font-style: bold;
}

/* スライダー共通 - コンパクト */
.slider-header {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1px;
    height: 14px;
    flex-shrink: 0;
}

.slider-value {
    font-size: 9px;
    color: rgba(76, 175, 255, 1);
    -unity-font-style: bold;
}

.lighting-slider {
    margin-top: 1px;
    margin-bottom: 2px;
    height: 16px;
    flex-shrink: 0;
}

.lighting-slider > .unity-base-slider__dragger {
    width: 14px;
    height: 14px;
    border-radius: 7px;
    background-color: white;
    border-width: 2px;
    border-color: rgba(76, 175, 255, 1);
    margin-top: -5px;
}

.lighting-slider > .unity-base-slider__tracker {
    height: 4px;
    border-radius: 2px;
    background-color: rgba(255, 255, 255, 0.2);
}

.lighting-slider > .unity-base-slider__drag-container > .unity-base-slider__dragger-border {
    display: none;
}

/* 色温度グラデーション - コンパクト */
.color-temp-gradient {
    height: 4px;
    border-radius: 2px;
    margin-bottom: 1px;
    background: linear-gradient(to right, #ff9500, #ffcc00, #ffffff, #aaccff, #6699ff);
    flex-shrink: 0;
}

/* ライト方向コントロール - コンパクト版 */
.light-direction-control {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    flex-shrink: 0;
    height: 70px;
}

.light-direction-background {
    width: 65px;
    height: 65px;
    border-radius: 32px;
    background-color: rgba(255, 255, 255, 0.1);
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.2);
    position: relative;
}

/* Issue #74: EWSN方向ラベル
   親: 65x65px, border: 1px → 内部(contentRect): 63x63px
   ラベル: 8x8px (フォントサイズ7pxに合わせる)
   水平中央: (63-8)/2 = 27.5 ≈ 27px
   垂直中央: (63-8)/2 = 27.5 ≈ 27px
*/
.direction-label {
    position: absolute;
    font-size: 7px;
    color: rgba(255, 255, 255, 0.5);
    -unity-text-align: middle-center;
    width: 8px;
    height: 8px;
}

.direction-n {
    top: 1px;
    left: 27px;
}

.direction-s {
    bottom: 1px;
    left: 27px;
}

.direction-e {
    right: 1px;
    top: 27px;
}

.direction-w {
    left: 1px;
    top: 27px;
}

/* Issue #74: ノブの初期位置
   contentRect基準で計算:
   - background: 65x65px, border: 1px → contentRect: 63x63px
   - contentRect中心: 31.5px
   - ノブ半径: 7px
   - 有効半径: min(31.5, 31.5) - 12 = 19.5px
   - 北方向（上）初期位置: left=31.5-7=24.5px, top=31.5-19.5-7=5px
*/
.light-direction-knob {
    position: absolute;
    width: 14px;
    height: 14px;
    border-radius: 7px;
    background-color: rgba(255, 200, 50, 1);
    border-width: 2px;
    border-color: white;
    left: 24px;
    top: 5px;
}

.light-direction-knob:hover {
    scale: 1.1;
}

/* Elevation コントロール - コンパクト */
.elevation-control {
    flex-direction: column;
    align-items: center;
    width: 40px;
}

.elevation-label {
    font-size: 7px;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 1px;
}

.elevation-slider {
    height: 50px;
    width: 20px;
}

.elevation-slider > .unity-base-slider__dragger {
    width: 10px;
    height: 10px;
    border-radius: 5px;
    background-color: white;
    border-width: 2px;
    border-color: rgba(76, 175, 255, 1);
    margin-left: -3px;
}

.elevation-slider > .unity-base-slider__tracker {
    width: 4px;
    border-radius: 2px;
    background-color: rgba(255, 255, 255, 0.2);
}

.elevation-value {
    font-size: 8px;
    color: rgba(76, 175, 255, 1);
    margin-top: 1px;
}

/* トグル行 - コンパクト */
.toggle-row {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 4px;
    height: 44px;
    flex-shrink: 0;
}

/* トグル行 - コンパクト版（上下パディング付き） */
.toggle-row-compact {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 4px;
    padding-top: 8px;
    padding-bottom: 8px;
    min-height: 28px;
    flex-shrink: 0;
}

.toggle-label {
    font-size: 9px;
    color: rgba(255, 255, 255, 0.8);
    flex-grow: 1;
    flex-shrink: 1;
}

/* Toggle の枠(40px)は compact 行に合わせる */
.toggle-row > Toggle,
.toggle-row-compact > Toggle {
    width: 40px;
    min-width: 40px;
    max-width: 40px;
    flex-grow: 0;
    flex-shrink: 0;
    justify-content: center;
    align-items: center;
    overflow: hidden;
}

/* Toggle 本体：スイッチの全体サイズ */
.shadow-toggle,
.ar-sync-toggle {
    width: 20px;
    height: 10px;
    min-width: 20px;
    min-height: 10px;
    max-width: 20px;
    max-height: 10px;
    padding: 0;
    margin: 0;
    flex-grow: 0;
    flex-shrink: 0;
    align-self: center;
    overflow: hidden;
}

/* ラベルは消す */
.shadow-toggle Label,
.ar-sync-toggle Label {
    display: none;
}

/* input は子孫指定で確実に当てる */
.shadow-toggle .unity-toggle__input,
.ar-sync-toggle .unity-toggle__input {
    width: 20px;
    height: 10px;
    min-width: 20px;
    min-height: 10px;
    max-width: 20px;
    max-height: 10px;
    padding: 0;
    margin: 0;
    flex-grow: 0;
    flex-shrink: 0;
    overflow: hidden;
}

/* checkmark = トラック（背景の帯） */
.shadow-toggle .unity-toggle__checkmark,
.ar-sync-toggle .unity-toggle__checkmark {
    width: 20px;
    height: 10px;
    min-width: 20px;
    min-height: 10px;
    max-width: 20px;
    max-height: 10px;
    padding: 0;
    margin: 0;
    border-radius: 5px;
    /* デフォ画像は消す（帯だけにする） */
    background-image: none;
    border-width: 0;
    background-color: rgba(255, 255, 255, 0.2);
    position: relative;
    overflow: hidden;
}

/* checkmark-image = ノブとして再利用 */
.shadow-toggle .unity-toggle__checkmark-image,
.ar-sync-toggle .unity-toggle__checkmark-image {
    display: flex;
    position: absolute;
    left: 1px;
    top: 1px;
    width: 8px;
    height: 8px;
    border-radius: 4px;
    background-color: white;
}

/* ON のとき帯の色 */
.shadow-toggle.unity-toggle--checked .unity-toggle__checkmark,
.ar-sync-toggle.unity-toggle--checked .unity-toggle__checkmark,
.shadow-toggle:checked .unity-toggle__checkmark,
.ar-sync-toggle:checked .unity-toggle__checkmark {
    background-color: rgba(76, 175, 255, 1);
}

/* ON のときノブを右へ */
.shadow-toggle.unity-toggle--checked .unity-toggle__checkmark-image,
.ar-sync-toggle.unity-toggle--checked .unity-toggle__checkmark-image,
.shadow-toggle:checked .unity-toggle__checkmark-image,
.ar-sync-toggle:checked .unity-toggle__checkmark-image {
    left: 11px;
}

/* ソフトネスボタン - コンパクト */
.softness-container {
    margin-top: 2px;
    flex-shrink: 0;
}

.softness-buttons {
    flex-direction: row;
    margin-top: 1px;
    height: 22px;
    flex-shrink: 0;
}

.softness-button {
    flex-grow: 1;
    height: 20px;
    margin-right: 3px;
    border-radius: 4px;
    background-color: rgba(255, 255, 255, 0.1);
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.15);
    color: rgba(255, 255, 255, 0.7);
    font-size: 8px;
}

.softness-button:last-child {
    margin-right: 0;
}

.softness-button:hover {
    background-color: rgba(255, 255, 255, 0.2);
}

.softness-button:active {
    background-color: rgba(76, 175, 255, 0.4);
    border-color: rgba(76, 175, 255, 0.8);
}

.softness-button.softness-selected {
    background-color: rgba(76, 175, 255, 0.4);
    border-color: rgba(76, 175, 255, 1);
    color: white;
    -unity-font-style: bold;
}

/* AR同期セクション - コンパクト（下部用・未使用） */
.ar-sync-section {
    padding-top: 4px;
    border-top-width: 1px;
    border-top-color: rgba(255, 255, 255, 0.1);
    flex-shrink: 0;
}

/* AR同期セクション - ヘッダー直下用 */
.ar-sync-section-top {
    margin-top: 2px;
    margin-bottom: 8px;
    padding-bottom: 0;
    flex-shrink: 0;
}

.ar-sync-description {
    font-size: 7px;
    color: rgba(255, 255, 255, 0.4);
    margin-top: 1px;
}

/* ================================
   シャドウパネルスタイル
================================ */

/* オーバーレイ（パネルコンテナ） */
.shadow-panel-overlay {
    position: absolute;
    left: 8px;
    right: 8px;
    bottom: 80px;
    background-color: transparent;
    display: none;
    flex-direction: column;
    justify-content: flex-end;
    align-items: stretch;
}

.shadow-panel-overlay.visible {
    display: flex;
}

/* メインパネル */
.shadow-panel {
    width: 100%;
    background-color: rgba(20, 20, 20, 0.95);
    border-radius: 10px;
    padding: 10px;
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.2);
    flex-direction: column;
}

/* ヘッダー */
.shadow-panel-header {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 8px;
    padding-bottom: 6px;
    border-bottom-width: 1px;
    border-bottom-color: rgba(255, 255, 255, 0.15);
    height: 20px;
}

.shadow-panel-title {
    font-size: 12px;
    color: white;
    -unity-font-style: bold;
}

.shadow-panel-close {
    width: 22px;
    height: 22px;
    border-radius: 11px;
    background-color: rgba(255, 255, 255, 0.15);
    border-width: 0;
    color: white;
    font-size: 12px;
    -unity-text-align: middle-center;
}

.shadow-panel-close:hover {
    background-color: rgba(255, 255, 255, 0.3);
}

.shadow-panel-close:active {
    background-color: rgba(76, 175, 255, 0.5);
}

/* セクション */
.shadow-section {
    margin-bottom: 8px;
}

/* シャドウスライダー */
.shadow-slider {
    margin-top: 2px;
    margin-bottom: 2px;
    height: 16px;
}

.shadow-slider > .unity-base-slider__dragger {
    width: 14px;
    height: 14px;
    border-radius: 7px;
    background-color: white;
    border-width: 2px;
    border-color: rgba(76, 175, 255, 1);
    margin-top: -5px;
}

.shadow-slider > .unity-base-slider__tracker {
    height: 4px;
    border-radius: 2px;
    background-color: rgba(255, 255, 255, 0.2);
}
